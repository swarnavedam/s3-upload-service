# Class containing main() method...
mainClass=

# Root of all human-authored source...
srcDir=src

# Root of all human-authored main Groovy source...
mainGroovySrcDir=src/main/groovy

# Root of all human-authored test Groovy source (Spock test cases, mock classes, etc)...
testGroovySrcDir=src/test/groovy

# Root of all human-authored main configuration source...
mainConfigSrcDir=src/main/config

# Meta-Inf dir, configurations to be deployed in META-INF
metaInfDir=META-INF

# The contents of this folder are directly copied/merged into the folder-structure of the build product (i.e. the JAR file)...
mainResourcesDir=src/main/resources

# Root of all human-authored test configuration source (configuration files for running unit tests)...
testConfigSrcDir=src/test/config

# Resources for testing
testResourcesDir=src/test/resources

# Library folder - JARs needed at both compile and runtime that will be provided by the environment...
# envLibDir=envlib

# Library root folder - contains JARs needed at both compile and runtime that will not be provided by the environment...
libDir=lib

# Library folder - JARs needed at both compile and runtime that will not be provided by the environment...
mainLibDir=lib/main

# Library folder - JARs needed to compile and run the unit tests...
testLibDir=lib/test

# Root of all compiled Java source...
buildDir=build

# Target folder for all compiled main Java source...
mainBuildDir=build/main

# Target folder for all compiled test Java source...
testBuildDir=build/test

# Target folder for packaged JAR file...
distDir=dist

# Root of all computer-generated files (except for compiled Java)...
genDir=generated

# Root of all computer-generated documentation...
genDocsDir=generated/docs

# Contains generated javadoc content...
genJavadocDir=generated/docs/javadoc

genGroovydocDir=generated/docs/groovydoc

# Root of all computer-generated source...
genSrcDir=generated/src

# Root of all computer-generated Java source...
genMainJavaSrcDir=generated/src/main/java

# Root of all computer-generated data...
genDataDir=generated/data

# Root of all computer-generated reports...
genReportsDir=generated/reports

# Root of unit test related report files...
genUnitTestReportsDir=generated/reports/unit-test

# Root of Cobertura report files...
genCoberturaReportsDir=generated/reports/coverage

# Root of codenarc report files
genCodeNardReportsDir=generated/reports/codenarc

# Contains raw unit test xml-based reports...
genRawUnitTestReportsDir=generated/reports/unit-test/raw-xml

# Contains formatted unit test reports...
genFormattedUnitTestReportsDir=generated/reports/unit-test/formatted

# Root of all instrumented Java class files...
genInstrumentedClassesDir=generated/instrumented-bin

# Contains Cobertura-instrumented Java class files...
genCoberturaInstrumentedClassesDir=generated/instrumented-bin/cobertura

# Location of generated Cobertura data file...
genCoberturaDataFileDir=generated/data/cobertura

# Name of Cobertua data file...
coberturaDataFileName=cobertura.ser

# contrib folder base
contribFolder=contrib

# code deploy zip folder
codeDeployZip=contrib/codedeploy

# Javadoc package names...
javadocPackageNames=com.welltok.hub.*

# Minimal properties for Apache Ivy
revision=1.0
descriptorFile=ivy.xml
ivyResolveConfigs=compile
ivyRetrievePattern=${mainLibDir}/[artifact]-[type]-[revision].[ext]
ivyRetrieveConfigs=compile
ivySettingsFile=ivy-settings.xml

